# 记账笔记 Android 项目 - 环境搭建指南

## 📋 环境要求

- **JDK**: JDK 17 (推荐使用 Temurin 17.0.2 或更高)
- **Android SDK**: Android SDK 34 (API Level 34)
- **Gradle**: Gradle 8.7 (已在项目中包含)
- **系统要求**: Linux/macOS/Windows

## 🚀 快速开始

### 1. 环境变量配置

项目已包含环境配置脚本 `set_env.sh`：

```bash
# 方式一：直接运行配置脚本
source /workspace/set_env.sh

# 方式二：手动设置环境变量
export JAVA_HOME=/root/.local/share/mise/installs/java/17.0.2
export PATH=$JAVA_HOME/bin:$PATH
export ANDROID_HOME=/root/android-sdk
export ANDROID_SDK_ROOT=/root/android-sdk
export PATH=$ANDROID_HOME/platform-tools:$ANDROID_HOME/cmdline-tools/latest/bin:$PATH
```

### 2. 构建项目

项目根目录已包含构建脚本 `build.sh`：

```bash
# 使用构建脚本
/workspace/build.sh

# 或直接使用 Gradle
./gradle-8.7/bin/gradle assembleDebug
```

### 3. APK 输出位置

构建完成后，APK 文件将生成在：
```
/workspace/app/build/outputs/apk/debug/app-debug.apk
```

## 🔧 详细环境配置

### JDK 17 安装

#### 使用 mise (推荐)
```bash
mise use java@17.0.2
```

#### 手动下载
从 [Adoptium](https://adoptium.net/) 下载 JDK 17 并设置：
```bash
export JAVA_HOME=/path/to/jdk-17
export PATH=$JAVA_HOME/bin:$PATH
```

### Android SDK 安装

#### 自动安装 (如果 ~/android-sdk 不存在)
SDK 工具已安装在 `~/android-sdk/`，包含：
- platform-tools
- platforms;android-34
- build-tools;34.0.0

#### 手动安装 SDK
```bash
# 接受协议
yes | ~/android-sdk/cmdline-tools/latest/bin/sdkmanager --licenses

# 安装必需组件
~/android-sdk/cmdline-tools/latest/bin/sdkmanager \
  "platform-tools" \
  "platforms;android-34" \
  "build-tools;34.0.0"
```

### Gradle

项目已包含 Gradle 8.7 在 `/workspace/gradle-8.7/`，无需额外安装。

## 📱 项目结构

```
/workspace/
├── app/                          # Android 应用模块
│   └── src/main/
│       ├── java/com/diarybook/
│       │   ├── ui/screen/        # 界面屏幕
│       │   │   ├── OcrRecognitionScreen.kt  # OCR 识别屏幕 ✨ 新增
│       │   │   ├── AddBillScreen.kt
│       │   │   ├── DetailScreen.kt
│       │   │   └── ...
│       │   ├── util/             # 工具类
│       │   │   ├── BillOcrRecognizer.kt      # OCR 识别核心 ✨ 新增
│       │   │   └── CategoryIconMapper.kt
│       │   ├── data/model/       # 数据模型
│       │   │   └── BillRecognitionResult.kt # OCR 结果 ✨ 新增
│       │   ├── navigation/       # 导航
│       │   │   └── AppNavHost.kt            # 路由配置 ✨ 更新
│       │   └── ...
│       └── AndroidManifest.xml
├── gradle-8.7/                   # Gradle 8.7
├── local.properties              # SDK 路径配置
├── build.sh                     # 构建脚本
└── set_env.sh                   # 环境变量配置
```

## ✨ OCR 功能说明

### 核心组件

1. **BillOcrRecognizer.kt** - OCR 识别引擎
   - 使用 Google ML Kit 中文识别
   - 智能提取金额、日期、商户
   - 自动分类建议

2. **OcrRecognitionScreen.kt** - 识别界面
   - 图片选择/拍照
   - 实时识别
   - 结果预览与编辑

3. **BillRecognitionResult.kt** - 识别结果数据类

### 使用流程

1. 在首页点击右上角 📷 相机图标
2. 选择相册图片或拍照
3. 系统自动识别账单信息
4. 确认或修改识别结果
5. 点击保存

## ⚠️ 常见问题

### Q: 构建失败，提示网络超时
A: 网络连接问题。Gradle 需要下载依赖库，建议：
- 使用稳定的网络连接
- 配置 Gradle 代理（如需要）
- 增加超时时间：
  ```bash
  export GRADLE_OPTS="-Dorg.gradle.internal.http.connectionTimeout=120000 -Dorg.gradle.internal.http.socketTimeout=120000"
  ```

### Q: 找不到 JDK 17
A: 确认 JAVA_HOME 设置正确：
```bash
export JAVA_HOME=/path/to/jdk-17
java -version  # 应显示 17.x.x
```

### Q: Android SDK 找不到
A: 检查 local.properties 文件：
```
sdk.dir=/root/android-sdk
```

## 📦 依赖说明

项目使用以下主要依赖：
- **Jetpack Compose**: UI 框架
- **Room**: 数据库
- **Google ML Kit**: 中文 OCR 识别
- **Coil**: 图片加载
- **Material 3**: 设计组件

所有依赖在 `gradle/libs.versions.toml` 中定义。

## 🎯 下一步

1. 确保网络连接稳定
2. 运行 `/workspace/build.sh` 构建项目
3. 在模拟器或真机上安装 APK
4. 测试 OCR 识图记账功能

## 📞 支持

如有问题，请检查：
1. 环境变量是否正确设置
2. 网络连接是否正常
3. 所有依赖是否成功下载
