# 云端部署指南

## 🚀 Android 应用云端运行与展示方案

### 方案一：提供 APK 下载（推荐）

#### 步骤 1：构建 APK
```bash
cd /workspace
source set_env.sh
./build.sh
```

#### 步骤 2：上传 APK
将生成的 APK 上传到：
- GitHub Releases
- 云存储服务（OSS/S3）
- 任何文件托管平台

#### 步骤 3：分享下载链接
用户下载后直接在 Android 设备安装

---

### 方案二：使用 Appetize.io 云端演示

1. 访问 [appetize.io](https://appetize.io/)
2. 上传 APK 文件
3. 获取演示链接分享给他人
4. 支持在浏览器中直接运行，无需安装

---

### 方案三：GitHub Actions 自动化构建

#### 创建 CI/CD 工作流

```yaml
# .github/workflows/build.yml
name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          
      - name: Grant execute permission
        run: chmod +x gradlew
        
      - name: Build Debug APK
        run: ./gradlew assembleDebug
        
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
```

---

### 方案四：Docker 容器化构建

#### 创建 Dockerfile
```dockerfile
# Dockerfile
FROM ubuntu:22.04

# 安装基础依赖
RUN apt-get update && apt-get install -y \
    curl \
    wget \
    unzip \
    git \
    openjdk-17-jdk \
    && rm -rf /var/lib/apt/lists/*

# 设置环境变量
ENV JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
ENV ANDROID_HOME=/opt/android-sdk
ENV ANDROID_SDK_ROOT=/opt/android-sdk
ENV PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools

# 安装 Android SDK
RUN mkdir -p /opt/android-sdk/cmdline-tools && \
    wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O /tmp/cmdline-tools.zip && \
    unzip -q /tmp/cmdline-tools.zip -d /opt/android-sdk/cmdline-tools && \
    mv /opt/android-sdk/cmdline-tools/cmdline-tools /opt/android-sdk/cmdline-tools/latest && \
    rm /tmp/cmdline-tools.zip

# 接受协议并安装 SDK 组件
RUN yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses && \
    $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
    "platform-tools" \
    "platforms;android-34" \
    "build-tools;34.0.0"

# 设置工作目录
WORKDIR /app

# 复制项目（构建时替换）
# COPY . /app

CMD ["./gradlew", "assembleDebug"]
```

#### 使用 Docker 构建
```bash
# 1. 构建镜像
docker build -t diarybook-builder .

# 2. 运行构建
docker run --rm \
  -v $(pwd):/app \
  -w /app \
  diarybook-builder \
  ./gradlew assembleDebug

# 3. 获取 APK
# APK 在 ./app/build/outputs/apk/debug/app-debug.apk
```

---

## 📱 在线测试平台推荐

### 1. **Appetize.io**
- ✅ 免费额度可用
- ✅ 浏览器直接运行
- ✅ 支持分享链接
- ✅ 无需安装任何软件

### 2. **Firebase Test Lab**
- ✅ Google 官方平台
- ✅ 真实设备测试
- ✅ 集成 CI/CD

### 3. **BrowserStack**
- ✅ 多设备测试
- ✅ 自动化测试
- ✅ 性能监控

---

## 🛠️ 使用当前环境的快速方法

### 步骤 1：确保环境正确
```bash
cd /workspace
./check_env.sh
```

### 步骤 2：尝试构建（如果网络允许）
```bash
source set_env.sh
./build.sh
```

### 步骤 3：如果构建成功
```bash
# 查看 APK 信息
ls -lh app/build/outputs/apk/debug/

# 获取 APK 大小
du -h app/build/outputs/apk/debug/app-debug.apk
```

---

## 🎯 推荐方案组合

### **对于展示/演示**
1. 在本地网络环境中成功构建 APK
2. 上传到 Appetize.io 获取在线演示链接
3. 同时提供 APK 下载链接

### **对于开发/测试**
1. 配置 GitHub Actions 自动构建
2. 每次推送自动构建 APK
3. 使用 GitHub Releases 发布新版本

---

## ⚠️ 注意事项

1. **首次构建**：需要稳定的网络下载依赖（约 200MB）
2. **构建时间**：首次 10-30 分钟，后续 1-3 分钟
3. **APK 大小**：预计 10-20MB
4. **最低 Android 版本**：API 29 (Android 10)

---

## 📞 需要帮助？

如果您需要：
- 配置 GitHub Actions
- 创建 Docker 容器
- 其他云端部署方案

请告诉我，我可以为您继续完善！
