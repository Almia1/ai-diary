

# 记账笔记 APP 开发与设计手册

**版本**：v1.0 (基础版)
**目标**：开发一款纯本地、轻量化、高颜值的记账应用，核心包含手动记账、多账本管理、图表统计及**截图识别记账**功能。

---

## 一、产品概述与核心规范

### 1. 产品定位
- **类型**：纯本地轻量化记账APP（无广告、无强制联网）。
- **性能指标**：
  - 安装包体积：≤ 10MB（需严格资源瘦身）。
  - 启动速度：冷启动 ≤ 2秒。
  - 支持系统：Android 10+ (API 29)，仅支持竖屏。

### 2. UI 视觉设计规范 (关键帧参考)

#### **整体视觉风格**
- **主色调**：线性渐变紫。
  - 起始色：`#6A5ACD`
  - 结束色：`#9370DB`
  - 渐变方向：从上到下（应用于顶部卡片、主要按钮）。
- **背景色**：纯白 `#FFFFFF`。
- **字体**：Roboto (无衬线字体)。
  - 正文：14sp / #333333
  - 小标题：16sp / #666666
  - **重点数据（金额/结余）**：24sp (加粗) 或 32sp (超大加粗) / `#6A5ACD`
- **圆角规则**：
  - 卡片：24px
  - 按钮/输入框：16px
  - 弹窗：20px
- **布局间距**：
  - 组件内间距：8dp
  - 组件外间距：16dp (页面左右边距)

#### **底部导航栏规范**
| 图标 | 文字 | 状态颜色 |
| :--- | :--- | :--- |
| 📊 | 明细 | 选中: `#6A5ACD` / 未选中: `#999999` |
| 📅 | 日历 | 选中: `#6A5ACD` / 未选中: `#909090` |
| 📈 | 统计 | 选中: `#6A5ACD` / 未选中: `#909090` |
| 💳 | 资产 | 选中: `#6A5ACD` / 未选中: `#909090` |
| 👤 | 我的 | 选中: `#6A5ACD` / 未选中: `#909090` |
| **规格**：图标 24dp，文字 12sp；选中时图标轻微放大 (1.1x)。

---

## 二、核心页面功能与交互设计

### 1. 明细页 (首页)
**UI结构参考**：
1.  **顶部状态栏 (16%)**：
    - 左上：账本切换下拉框（显示当前账本名，如“日常账本”）。
    - 居中：月份显示（“2026年04月”，点击可切换）。
    - 右上：设置图标（⚙️）。
2.  **收支总览卡片 (20%)**：
    - **视觉**：宽屏占比 90%，高度 120dp，线性渐变紫背景，圆角 24px。
    - **内容**：
        - 左侧：本月结余（32sp，白色，加粗）。
        - 右侧：本月支出（红色 `#FF4444`）、本月收入（绿色 `#4CAF50`）。
        - 右侧箭头：点击跳转月度分析。
3.  **快捷功能入口 (12%)**：
    - 5个图标横向排列（微信记账、定时记账、共享记账、债务管理、历史账单）。
    - **新增**：在卡片右上角增加“相机图标”，点击触发【识图记账】。
4.  **账单列表 (剩余高度)**：
    - 按日期分组，显示具体账单项。

### 2. 日历页
**UI结构参考**：
1.  **顶部导航**：月份选择 + 年份显示。
2.  **日历视图 (30%)**：
    - 星期栏：日 一 二 三 四 五 六。
    - 日期格子：
        - **今日**：浅紫底色 `#F5F0FF` + 白色“今”字。
        - **有账单日期**：日期右上角红色小圆点 (8dp) + 显示当日总金额 (10sp)。
3.  **数据统计栏**：显示当月总支出、总收入、月结余。
4.  **底部操作**：“+ 记一笔”悬浮按钮。

### 3. 识图记账专项流程 (v1.0 核心亮点)
**交互流程**：
1.  **入口**：首页右上角相机图标。
2.  **选择图片**：调用系统相册/相机（需申请权限）。
3.  **识别中**：全屏遮罩，显示“正在识别账单...”。
4.  **结果预览页 (ModalBottomSheet)**：
    - **布局**：宽度 90%，圆角 20px，白色背景。
    - **内容**：
        - 上方：原图缩略图 (80x80dp)。
        - 中间：金额输入框（自动填入识别值）、分类选择（自动匹配）、备注（自动填入商户名）。
        - 底部：取消 / 保存按钮（渐变紫）。

**技术实现逻辑**：
- **OCR引擎**：Google ML Kit Text Recognition (Chinese离线版)。
- **正则提取规则**：
  - 金额：`(?:¥|￥)?(\d+\.\d{2})\s*元?`
  - 日期：`\d{4}-\d{2}-\d{2}`
  - 商户：`交易对方：\s*(.+)` 或 `商品说明：\s*(.+)`
- **自动分类**：建立关键词映射表（如“星巴克” -> “餐饮”）。

---

## 三、数据模型设计 (Room DB)



#### 1. 账单表（tb_bill）
| 字段名       | 数据类型     | 约束                                       | 说明               |
|--------------|--------------|--------------------------------------------|--------------------|
| id           | INTEGER      | PRIMARY KEY AUTOINCREMENT                  | 账单唯一ID         |
| book_id      | INTEGER      | NOT NULL, FOREIGN KEY REFERENCES tb_book(id) | 关联账本ID         |
| type         | INTEGER      | NOT NULL, CHECK(type IN (0,1))             | 0支出/1收入        |
| amount       | DECIMAL(10,2)| NOT NULL, CHECK(amount > 0)                | 金额（>0，2位小数）|
| category_id  | INTEGER      | NOT NULL, FOREIGN KEY REFERENCES tb_category(id) | 分类ID |
| date         | DATE         | NOT NULL, CHECK(date <= DATE('now'))       | 账单日期（YYYY-MM-DD，禁止未来） |
| remark       | VARCHAR(200) |                                            | 备注               |
| image_path   | VARCHAR(200) |                                            | 小票图片路径       |
| create_time  | DATETIME     | NOT NULL                                   | 创建时间           |
| update_time  | DATETIME     | DEFAULT CURRENT_TIMESTAMP                  | 最后修改时间       |

#### 2. 定时记账表（补充，tb_recurring_bill）
| 字段名         | 数据类型     | 约束                                       | 说明               |
|----------------|--------------|--------------------------------------------|--------------------|
| id             | INTEGER      | PRIMARY KEY AUTOINCREMENT                  | 定时规则ID         |
| book_id        | INTEGER      | NOT NULL, FOREIGN KEY REFERENCES tb_book(id) | 关联账本ID         |
| type           | INTEGER      | NOT NULL, CHECK(type IN (0,1))             | 0支出/1收入        |
| amount         | DECIMAL(10,2)| NOT NULL, CHECK(amount > 0)                | 固定金额           |
| category_id    | INTEGER      | NOT NULL                                   | 分类ID             |
| cycle_type     | INTEGER      | NOT NULL, CHECK(cycle_type IN (1,2,3))     | 1每日/2每周/3每月  |
| cycle_value    | VARCHAR(20)  | NOT NULL                                   | 周期值（如每周三、每月5号） |
| start_date     | DATE         | NOT NULL                                   | 开始日期           |
| end_date       | DATE         |                                            | 结束日期（可选）|
| is_auto_create | INTEGER      | DEFAULT 0, CHECK(is_auto_create IN (0,1))  | 1自动生成/0仅提醒  |
| remind_time    | TIME         | NOT NULL                                   | 提醒时间（如08:00）|


### 3. 分类表（tb_category）
| 字段名       | 数据类型 | 约束                                       | 说明                     |
|--------------|----------|--------------------------------------------|--------------------------|
| id           | INTEGER  | PRIMARY KEY AUTOINCREMENT                  | 分类唯一ID               |
| book_id      | INTEGER  | NOT NULL, FOREIGN KEY REFERENCES tb_book(id) | 关联账本ID（0为全局分类） |
| type         | INTEGER  | NOT NULL, CHECK(type IN (0,1))             | 0支出/1收入              |
| name         | VARCHAR(20) | NOT NULL, CHECK(LENGTH(name) BETWEEN 2 AND 20) | 分类名称（2-20字）|
| icon         | VARCHAR(50) | NOT NULL                                   | 图标名称/路径（如ic_food） |
| color        | VARCHAR(7) | NOT NULL DEFAULT '#6A5ACD'                 | 分类颜色（十六进制）|
| is_default   | INTEGER  | DEFAULT 0, CHECK(is_default IN (0,1))      | 是否系统默认分类（1不可删） |
| sort         | INTEGER  | DEFAULT 0                                  | 显示排序值（越小越靠前） |
| create_time  | DATETIME | NOT NULL                                   | 创建时间                 |
| update_time  | DATETIME | DEFAULT CURRENT_TIMESTAMP                  | 最后修改时间             |
**索引**：`CREATE INDEX idx_category_book_type ON tb_category(book_id, type);`

### 4. 预算表（tb_budget）
| 字段名       | 数据类型     | 约束                                       | 说明                     |
|--------------|--------------|--------------------------------------------|--------------------------|
| id           | INTEGER      | PRIMARY KEY AUTOINCREMENT                  | 预算唯一ID               |
| book_id      | INTEGER      | NOT NULL, FOREIGN KEY REFERENCES tb_book(id) | 关联账本ID               |
| year         | INTEGER      | NOT NULL, CHECK(year >= 2000 AND year <= 2100) | 预算年份（2000-2100）|
| month        | INTEGER      | NOT NULL, CHECK(month BETWEEN 1 AND 12)    | 预算月份（1-12）|
| type         | INTEGER      | NOT NULL, CHECK(type IN (0,1))             | 0总预算/1分类预算        |
| category_id  | INTEGER      | NULL, FOREIGN KEY REFERENCES tb_category(id) | 分类ID（type=1时非空） |
| amount       | DECIMAL(10,2)| NOT NULL, CHECK(amount >= 0)               | 预算金额（≥0，2位小数） |
| used_amount  | DECIMAL(10,2)| DEFAULT 0, CHECK(used_amount >= 0)         | 已使用金额               |
| create_time  | DATETIME     | NOT NULL                                   | 创建时间                 |
| update_time  | DATETIME     | DEFAULT CURRENT_TIMESTAMP                  | 最后修改时间             |
**索引**：`CREATE INDEX idx_budget_book_year_month_type ON tb_budget(book_id, year, month, type);`

### 5. 债务表（tb_debt）
| 字段名       | 数据类型     | 约束                                       | 说明                     |
|--------------|--------------|--------------------------------------------|--------------------------|
| id           | INTEGER      | PRIMARY KEY AUTOINCREMENT                  | 债务唯一ID               |
| book_id      | INTEGER      | NOT NULL, FOREIGN KEY REFERENCES tb_book(id) | 关联账本ID               |
| name         | VARCHAR(50)  | NOT NULL, CHECK(LENGTH(name) BETWEEN 2 AND 50) | 债务名称（如“花呗”）|
| type         | INTEGER      | NOT NULL, CHECK(type IN (0,1))             | 0欠款/1借款              |
| amount       | DECIMAL(10,2)| NOT NULL, CHECK(amount > 0)                | 债务金额（>0）|
| due_date     | DATE         | NOT NULL                                   | 到期日期（YYYY-MM-DD）|
| paid_amount  | DECIMAL(10,2)| DEFAULT 0, CHECK(paid_amount >= 0)         | 已还款/已收回金额        |
| status       | INTEGER      | DEFAULT 0, CHECK(status IN (0,1,2))        | 0未结清/1已结清/2逾期    |
| remark       | VARCHAR(200) |                                            | 备注                     |
| create_time  | DATETIME     | NOT NULL                                   | 创建时间                 |
| update_time  | DATETIME     | DEFAULT CURRENT_TIMESTAMP                  | 最后修改时间             |
**索引**：`CREATE INDEX idx_debt_book_status_due_date ON tb_debt(book_id, status, due_date);`

### 6. 用户设置表（tb_user_setting）
| 字段名           | 数据类型 | 约束                                       | 说明                     |
|------------------|----------|--------------------------------------------|--------------------------|
| id               | INTEGER  | PRIMARY KEY AUTOINCREMENT                  | 设置ID                   |
| theme_mode       | INTEGER  | DEFAULT 0, CHECK(theme_mode IN (0,1,2))    | 0浅色/1深色/2跟随系统    |
| auto_backup      | INTEGER  | DEFAULT 1, CHECK(auto_backup IN (0,1))     | 0关闭自动备份/1开启      |
| backup_time      | TIME     | DEFAULT '23:00'                            | 自动备份时间             |
| currency_symbol  | VARCHAR(5) | DEFAULT '¥'                                | 货币符号（¥/$等）|
| remind_debt      | INTEGER  | DEFAULT 1, CHECK(remind_debt IN (0,1))     | 0关闭债务提醒/1开启      |
| remind_recurring | INTEGER  | DEFAULT 1, CHECK(remind_recurring IN (0,1)) | 0关闭定时记账提醒/1开启  |
| cache_auto_clean | INTEGER  | DEFAULT 1, CHECK(cache_auto_clean IN (0,1)) | 0关闭自动清理缓存/1开启  |
| update_time      | DATETIME | DEFAULT CURRENT_TIMESTAMP                  | 最后修改时间             |



---

## 四、技术实现约束

### 1. 核心栈
- **语言**：Kotlin 1.9.0
- **UI**：Jetpack Compose 1.5.4 (Material3)
- **架构**：MVVM (ViewModel + StateFlow/Flow)
- **数据库**：Room 2.5.2 + SQLCipher 4.5.5 (加密)

### 2. 性能与安全优化
- **启动优化**：使用 Splash Screen API，延迟初始化非核心组件（如图表库）。
- **包体积控制**：
  - 图片资源仅保留 xxhdpi，其余使用矢量图。
  - 仅保留中文语言资源。
  - 开启 R8 混淆。
- **数据安全**：
  - 数据库存储在私有目录，使用 SQLCipher 加密。
  - 核心页面（如备份页）设置 `FLAG_SECURE` 防截屏。

### 3. 权限管理
| 权限 | 用途 | 拒绝处理 |
| :--- | :--- | :--- |
| 存储 (读/写) | 识图记账、数据备份 | 提示“无法使用图片功能，请授权” |
| 相机 | 拍照记账 | 提示“请手动选择图片” |

---

## 五、验收标准 (Checklist)

1.  **UI还原度**：
    - 首页顶部卡片必须为线性渐变紫。
    - 所有卡片圆角必须为 24dp。
    - 识图记账入口必须在首页右上角。
2.  **功能完整性**：
    - 识图记账能正确识别微信/支付宝截图中的金额和日期。
    - 预览弹窗能手动修改分类和金额。
3.  **性能指标**：
    - 安装包安装后体积 ≤ 10MB。
    - 首次启动时间 ≤ 2秒。

---

## 六、附录：UI参考图索引
*(此处建议插入具体的UI截图文件名或Base64，供开发对照)*
- `home_screen.png`：明细页设计图
- `calendar_screen.png`：日历页设计图
![[calendar_screen.png.jpg]]


![[home_screen.png.jpg]]